const fst = 'jaan';
const lst = 'pakhi';

const name = `${fst} ${lst}`
// const name = fst + ' ' + lst;
console.log(name);

const a = 10;
const b = 20;

const sum = `The Sum Of Your Result ${a + b}`

console.log(sum)